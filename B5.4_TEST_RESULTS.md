# B5.4 Verification Results

Phase: B5.4 — Document-Level Access Control

## Executed in ChatGPT runtime

### Pytest
Result: **59 passed**

B5.4-specific coverage includes:
- unrestricted documents continue to inherit organization-level access;
- activating an ACL hides the document from non-allowlisted same-org readers;
- direct restricted-document access returns generic 404;
- the first ACL must be `manage` and preserve current-manager control;
- read ACL grants read but not management;
- manage ACL gates archive/mutation for same-organization managers;
- last-manager revocation cannot leave a restricted read-only orphan;
- removing the final ACL returns to inherited mode;
- grant/revoke actions are audited;
- role targets must have the required canonical base permission;
- roles assigned only outside the document organization scope are rejected;
- `access.manage` can recover ACL administration without gaining content access;
- a `documents.manage`-only user can upload successfully without a post-commit
  `documents.read` failure.

### Python compileall
Result: **passed**

### Alembic PostgreSQL offline SQL generation
Result: **passed through revision 20260910_0008**

## Ruff / Mypy
Repository configuration remains present, but the ChatGPT runtime does not have
`ruff` or `mypy` installed. No success claim is made for those tools.
