# B5.6 Verification Results

Phase: B5.6 — Core Write APIs
Baseline: Claude-approved B5.5 implementation package supplied by the user.

## Executed in ChatGPT runtime

### Pytest
Result: **90 passed**

Dedicated B5.6 write/security coverage includes:
- descendant Organization creation with B1 hierarchy validation
- SELF-only organization manager cannot create an unauthorized child
- business write rollback when Audit recording fails before commit
- parent deactivation blocked while active children exist
- inactive organization reactivation through an authorized active ancestor
- Person creation + initial organization relationship + Audit
- shared Person global mutation requires `people.manage` across all active relationships
- privileged User identity cannot be mutated by an actor who does not dominate its access footprint
- Person deactivation cannot bypass that privileged-user protection
- User creation hashes passwords and never audits plaintext/hash material
- admin password reset is audited without password/hash data
- usernames containing `@` are rejected to preserve deterministic login routing
- custom roles are organization-owned
- system/legacy-global role definitions are immutable through runtime APIs
- role permission grant is capability-bounded
- role permission revoke is audited
- role assignments require active Person relationship at the assignment root organization
- `SELF_AND_DESCENDANTS` assignment requires actor authority across every covered active descendant
- access-assignment reactivation rechecks user/person/relationship/role-owner boundaries
- person relationship deactivation is blocked while active access is rooted at that organization
- last effective `access.manage` holder cannot be removed by assignment deactivation

### Compileall
Result: **passed** for `app` and `tests`.

### OpenAPI generation
Result: **passed**.
- OpenAPI generated successfully.
- 46 API paths present.
- Required B5.6 Organization / People / Users / Access paths present.

### Alembic PostgreSQL offline SQL generation
Result: **passed through revision `20260910_0011`**.

B5.6 migration:
- adds nullable `roles.organization_id`
- FK -> organizations (`RESTRICT`)
- adds `ix_roles_organization_id`

### Ruff / Mypy
Both remain configured in the repository, but this ChatGPT runtime does not
have the `ruff` or `mypy` packages installed. No pass claim is made for them.

### Live PostgreSQL execution
Not claimed. Migration SQL generation was verified against PostgreSQL dialect,
but a live PostgreSQL migration run is still recommended in the normal local/
staging environment before production deployment.
