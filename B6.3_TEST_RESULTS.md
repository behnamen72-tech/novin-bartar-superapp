# B6.3 Test Results — Main Layout & Navigation

Baseline: B6.2 Login & Session full-review package.

## Backend regression

- `pytest -q`: **99 passed**
- `python -m compileall -q app tests scripts`: **passed**
- OpenAPI generation with isolated SQLite configuration: **48 paths**
- Alembic PostgreSQL offline SQL through migration `20260910_0012`: **passed**

No backend source or migration was changed by B6.3.

## Frontend verification

### TypeScript syntax

All frontend `.ts` / `.tsx` source files except declaration files were parsed with the TypeScript compiler API:

- files parsed: **32**
- syntax diagnostics: **0**

### Strict semantic check with environment stubs

A temporary local declaration shim for unavailable React/Next package types was used only to let the globally installed TypeScript compiler semantically check repository-local types and strict-mode code. The shim was removed immediately after the check.

Result: **passed**.

This is not claimed as a substitute for a real dependency-installed `npm run typecheck`.

### B6.3 navigation runtime checks

`frontend/scripts/check-b63-navigation.cjs` was executed with TypeScript available from the runtime.

Result: **12 assertions passed**.

Coverage includes:

- valid/invalid view-key parsing
- URL deep-link parsing and invalid fallback
- dashboard/location generation
- permission-gated Organization visibility
- isolated Audit visibility
- combined visible-navigation filtering

### Package/static invariants

Verified in source:

- navigation permission definitions are centralized in `lib/navigation.ts`
- `aria-current="page"` is applied to active navigation
- mobile drawer supports explicit close, backdrop, Escape key, and closes on navigation
- browser Back/Forward uses `popstate`
- invalid or unauthorized requested views fall back to Dashboard
- no access/refresh token handling was moved into B6.3 client code

## npm environment limitation

`npm install --ignore-scripts --no-audit --no-fund` was attempted but timed out in the isolated runtime.

Therefore the following are **not** claimed as passed:

- `npm run typecheck` with real React/Next package types
- `npm run lint`
- `npm run build`

Run these in the normal development environment after dependencies are installed.

## Final ZIP re-extract verification

After creating the full B6.3 review ZIP, it was extracted into a fresh directory and verified again:

- Backend pytest: **99 passed**
- Backend compileall: **passed**
- OpenAPI: **48 paths**
- PostgreSQL Alembic offline SQL through 0012: **passed**
- `npm run check:b63`: **12 assertions passed**
- TS/TSX syntax: **32 files, 0 diagnostics**
- strict repository-local semantic TypeScript check with temporary React/Next environment stubs: **passed**
- `backend/app/core/documents/storage/` source package present in the archive: **verified**
