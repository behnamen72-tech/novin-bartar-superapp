# B6.1 Test Results

## Backend regression

- Command: `python -m pytest -q`
- Result: **90 passed**

## Backend compile/OpenAPI/migrations

- `python -m compileall -q app tests`: **passed**
- OpenAPI generation: **passed**, 46 paths
- PostgreSQL Alembic offline SQL generation through revision `20260910_0011`: **passed**

## Frontend source syntax

- TypeScript/TSX files were parsed/transpiled with the installed TypeScript compiler API.
- Result: **24 TS/TSX files checked, 0 syntax diagnostics**

## Frontend dependency-based checks

`npm install --no-audit --no-fund` was attempted in the isolated execution environment but timed out before dependencies were installed. Therefore the following are **not claimed as passed in this environment**:

- `npm run typecheck`
- `npm run lint`
- `npm run build`

These commands remain required on the normal development machine after `npm install`.

## Notes

No database migration is introduced by B6.1.
