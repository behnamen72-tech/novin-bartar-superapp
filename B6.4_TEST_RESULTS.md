# B6.4 Test Results — Operational Dashboard

Baseline: B6.3 Main Layout & Navigation full-review package.

## Backend regression

B6.4 does not change backend source, models, migrations, or authorization logic.
The complete backend suite was still rerun against the cumulative package:

- `pytest -q`: **99 passed**
- `python -m compileall -q app tests scripts`: **passed**
- OpenAPI generation with isolated SQLite configuration: **48 paths**
- Alembic PostgreSQL offline SQL through migration `20260910_0012`: **passed**

## Frontend verification

### B6.3 navigation regression

`npm run check:b63`:

- **12 assertions passed**

### B6.4 deterministic dashboard checks

`npm run check:b64`:

- **11 assertions passed**

Coverage includes:

- dashboard data capabilities require the exact backend read permissions
- a manage-only permission does not cause the dashboard to call a read endpoint
- Organization/People/User active and total summaries
- document expiration/expired summaries
- assignment and unique-role summaries
- shared Audit/resource labels
- expiration-label behavior

### TypeScript syntax

All frontend `.ts` / `.tsx` source files except declaration files were parsed with the globally available TypeScript compiler API:

- files parsed: **36**
- syntax diagnostics: **0**

### Strict repository-local semantic check

A temporary declaration shim for unavailable React/Next package types was used only to let the globally installed TypeScript compiler check repository-local strict-mode semantics. The shim and temporary config were removed immediately after the run.

Result: **passed**.

This is not claimed as a substitute for a normal dependency-installed Next.js typecheck.

## npm environment limitation

`npm install --ignore-scripts --no-audit --no-fund` was attempted and timed out in the isolated runtime.

Therefore the following are **not** claimed as passed:

- `npm run typecheck` with real React/Next package types
- `npm run lint`
- `npm run build`

These should be run in the normal local development environment after dependencies are installed.

## Static security/integration checks

Verified in source:

- Dashboard never receives or stores access/refresh token values.
- New document BFF routes use `proxyAuthenticatedGet()`.
- Dashboard fetch gating uses read permissions matching the actual FastAPI GET endpoints.
- FastAPI remains authoritative for Organization Scope and Document ACL filtering.
- Dashboard partial failures do not turn failed data into a false zero metric.
- Document attention response is bounded at 200 and the UI signals a lower-bound count when the cap is reached.
- B6.2 session refresh/error path remains unchanged.

## Final ZIP re-extract verification

After creating the full B6.4 review ZIP, it was extracted into a fresh directory and the cumulative package was checked again:

- Backend pytest: **99 passed**
- Backend compileall: **passed**
- OpenAPI: **48 paths**
- PostgreSQL Alembic offline SQL through 0012: **passed**
- `npm run check:b63`: **12 assertions passed**
- `npm run check:b64`: **11 assertions passed**
- TS/TSX syntax: **36 files, 0 diagnostics**
- strict repository-local semantic TypeScript check with temporary React/Next stubs: **passed**
- `backend/app/core/documents/storage/` source package in archive: **verified**
